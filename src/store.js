import { defineStore } from "pinia";
const appStore = defineStore("app", {
  // other options...
  state: () => {
    return {
      
    };
  },
  persist: true,
});
export default appStore;
